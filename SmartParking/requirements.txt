# NeoSmart runtime dependencies (platform-agnostic core).
#
# Pinned to the versions that were in use during diploma development
# (python 3.11+). For Windows-only capture scripts (legacy tools),
# also install `requirements-win.txt`. For development tooling
# (tests, linting, experiment tracking), install `requirements-dev.txt`.
#
# Torch is intentionally unpinned here: CUDA/CPU wheels differ per
# host. Install torch/torchvision matching your platform first, then
# `pip install -r requirements.txt`. Ultralytics will pull a sane
# version if none is present.

# Web framework
fastapi==0.128.6
uvicorn[standard]==0.40.0
python-multipart==0.0.22
jinja2==3.1.6
websockets==16.0

# Configuration
pydantic==2.12.5
pydantic-settings==2.6.1
PyYAML==6.0.2

# Computer vision
opencv-python==4.11.0.86
numpy==2.2.6
ultralytics==8.3.150
cvzone==1.6.1

# License plate OCR (PlateScanner / parseq via torch.hub)
pytorch-lightning==2.6.1
timm==1.0.26
nltk==3.9.4

# Tracking (SORT)
filterpy==1.4.5
